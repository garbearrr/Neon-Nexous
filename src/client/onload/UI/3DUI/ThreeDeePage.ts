import { Players } from "@rbxts/services";

/*
const Player = Players.LocalPlayer;
const PlayerGui = Player.WaitForChild("PlayerGui") as StarterGui;
const SurfaceGui = PlayerGui.WaitForChild("ThreeDUI") as StarterGui["ThreeDUI"];

export type Template3DPage = typeof SurfaceGui["MainFrame"]["Scanlines"]["Shop"];

export abstract class ThreeDeePage {
    public readonly Page: Template3DPage;

    protected constructor(Page: Template3DPage) {
        this.Page = Page;
    }

    public abstract OnOpen(): void;

    public abstract OnClose(): void;
}
*/